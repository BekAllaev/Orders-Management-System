<UserControl x:Class="Settings.OrdersSettings.MainViewSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:propeties="clr-namespace:Settings.Properties"
             xmlns:local="clr-namespace:Settings.OrdersSettings"
             mc:Ignorable="d" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Choose main view"
                   Margin="10"
                   FontSize="28"/>

        <ComboBox Grid.Row="1"
                  Margin="5 0 5 0"
                  ItemsSource="{Binding AvailableViews}"
                  SelectedValue="{Binding SelectedView,Mode=TwoWay}">
        </ComboBox>
    </Grid>
</UserControl>
