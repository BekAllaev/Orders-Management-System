<UserControl x:Class="Dashboard.OrdersStatistic.OrderStatisticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:infrastructure="clr-namespace:Infrastructure;assembly=Infrastructure"
             xmlns:local="clr-namespace:Dashboard.OrdersStatistic"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Orders" 
                   FontSize="32"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="10 10 0 0"/>
        <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
            <Button Content="Manage"
                Command="{x:Static infrastructure:GlobalCommands.NavigateToCompositeCommand}"
                CommandParameter="OrdersMainView"
                Margin="5"
                FontSize="28"/>
        </StackPanel>


    </Grid>
</UserControl>
